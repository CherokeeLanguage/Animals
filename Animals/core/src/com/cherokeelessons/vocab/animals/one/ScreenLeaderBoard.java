package com.cherokeelessons.vocab.animals.one;

/*
 * JSONP to server
 * "nick" + "level" + "score" + "uniqid hash for device stored in non-volatile preferences"
 */

public class ScreenLeaderBoard extends ScreenGameCore {
	public ScreenLeaderBoard(CherokeeAnimals game) {
		super(game);
	}
}
